package application;

/* Функции:
 * set_TV_Resume - заполнение TableView.
 */
public interface Controller {
	
	/* Функция заполнения TableView
	 * Формальные параметры:
	 * query - параметры запроса.
	 */
	void set_TV_Resume(String query);
}
